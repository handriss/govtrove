{
  "name": "govtrove-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end checks driven through the real UI. Kept out of frontend/ so Playwright never becomes a build dependency of the app.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
